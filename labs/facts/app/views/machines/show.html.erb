<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @machine.name %>
</p>

<p>
  <strong>Region:</strong>
  <%= @machine.datacenter.try(:region).try(:name) %>
</p>

<p>
  <strong>Datacenter:</strong>
  <%= @machine.datacenter.try(:name) %>
</p>

<%= link_to 'Edit', edit_machine_path(@machine) %> |
<%= link_to 'Back', machines_path %>
