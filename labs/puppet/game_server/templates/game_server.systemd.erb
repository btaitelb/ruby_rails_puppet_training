[Unit]
Description=Ruby Game Server
Wants=basic.target
After=basic.target network.target

[Service]
Environment=GEM_HOME=<%= @gem_home %>
WorkingDirectory=/tmp
ExecStart=<%= @gem_home %>/bin/deck_server
User=<%= @user %>
KillMode=process
Restart=on-failure

[Install]
WantedBy=multi-user.target
